__pycache__/
*.py[cod]
*$py.class

# virtual environments
.venv/
venv/
env/
ENV/

# local export folders (regenerated by tools/nest, /api/export, etc.)
output/
# `out/` was the old name (renamed 22 Sep 2026). Still ignored so a stale
# folder left behind on a machine does not turn up as untracked.
out/

# snapshot baseline: generated per machine off a known-good tree, never shared
baseline.json

# editor/OS cruft
.vscode/
.idea/
Thumbs.db
.DS_Store
